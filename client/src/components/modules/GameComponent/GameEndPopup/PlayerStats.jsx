import React, { useState, useEffect } from "react";

/** Player Stats
 * @prop {username} username
 * @prop {score} score
 * @prop {rank} rank
 * @prop {lettersPlayed} wordsPlayed
 * @prop {wordsFormed} wordsFormed
 */

const PlayerStats = (props) => {};
